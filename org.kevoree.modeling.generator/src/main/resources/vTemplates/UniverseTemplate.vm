package ${context.metaModelPackage};

public class ${context.metaModelName}Universe extends org.kevoree.modeling.api.abs.AbstractKUniverse<${context.metaModelName}View, ${context.metaModelName}Universe, ${context.metaModelName}Model> {

    protected ${context.metaModelName}Universe(org.kevoree.modeling.api.KModel p_model, long key) {
        super(p_model, key);
    }

    @Override
    protected ${context.metaModelName}View internal_create(long timePoint) {
        return new ${context.metaModelPackage}.impl.${context.metaModelName}ViewImpl(timePoint,(org.kevoree.modeling.api.abs.AbstractKUniverse)this);
    }

}

#* @vtlvariable name="context" type="org.kevoree.modeling.generator.GenerationContext" *#